<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Pet Profile - <%= pet.name %></title>
  <link rel="stylesheet" href="/css/home.css" />
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #fffde7;
      margin: 0;
      padding: 0;
      color: #333;
    }

    header {
      background-color: #f2d707;
      padding: 1.5rem 2rem;
      text-align: center;
      border-bottom: 3px solid #e0c200;
    }

    header h1 {
      margin: 0;
      font-size: 2rem;
      color: #000;
    }

    .container {
      max-width: 800px;
      margin: 2rem auto;
      padding: 2rem;
      background-color: #ffffff;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .card {
      padding: 1.5rem;
      border-radius: 8px;
      background-color: #fff9c4;
      box-shadow: inset 0 0 0 1px #f2d707;
    }

    .card h3 {
      margin-top: 0;
      margin-bottom: 1rem;
      color: #222;
    }

    .card p {
      margin: 0.5rem 0;
      font-size: 1rem;
    }

    .card img {
      display: block;
      margin-top: 1rem;
      max-width: 100%;
      height: auto;
      border-radius: 8px;
      border: 2px solid #f2d707;
    }

    .action-button,
    button {
      display: inline-block;
      margin: 1rem 0.5rem 0 0;
      padding: 0.7rem 1.4rem;
      background-color: #f2d707;
      color: #000;
      text-decoration: none;
      border: none;
      border-radius: 6px;
      font-weight: bold;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .action-button:hover,
    button:hover {
      background-color: #e0c200;
    }

    form {
      display: inline;
    }
  </style>
</head>
<body>
  <header>
    <h1><%= pet.name %> - Pet Profile</h1>
  </header>

  <div class="container">
    <div class="card">
      <h3>Details</h3>
      <p><strong>Breed:</strong> <%= pet.breed %></p>
      <p><strong>Age:</strong> <%= pet.age %> years</p>
      <p><strong>Weight:</strong> <%= pet.weight %> kg</p>
      <p><strong>Color:</strong> <%= pet.color %></p>
      <p><strong>Gender:</strong> <%= pet.gender %></p>
      <p><strong>Birthday:</strong> <%= pet.birthday.toDateString() %></p>
      <p><strong>Spayed/Neutered:</strong> <%= pet.spayed === 'Yes' ? 'Yes' : 'No' %></p>
      <p><strong>Notes:</strong> <%= pet.notes %></p>
      <% if (pet.photo) { %>
        <img src="<%= pet.photo %>" alt="Pet Photo">
      <% } %>
    </div>

    <a href="/home" class="action-button">← Back to Dashboard</a>
    <a href="/pet/edit/<%= pet._id %>" class="action-button">Edit Profile</a>

    <form action="/pet/delete/<%= pet._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this pet?');">
      <button type="submit">Delete Profile</button>
    </form>
  </div>
</body>
</html>